# Copyright @ 2021 Thought Machine Group Limited. All rights reserved.
---
resources:
  - type: WORKFLOW_DEFINITION_VERSION
    id: WALLET_APPLICATION
    activate: false
    payload: |
      workflow_definition_version:
          workflow_definition_id: WALLET_APPLICATION
          specification: '@{wallet_application.yaml}'
  - type: WORKFLOW_DEFINITION_VERSION
    id: WALLET_AUTO_TOP_UP_SWITCH
    activate: true
    payload: |
      workflow_definition_version:
          workflow_definition_id: WALLET_AUTO_TOP_UP_SWITCH
          specification: '@{wallet_auto_top_up_switch.yaml}'
  - type: WORKFLOW_DEFINITION_VERSION
    id: WALLET_CLOSURE
    activate: true
    payload: |
      workflow_definition_version:
          workflow_definition_id: WALLET_CLOSURE
          specification: '@{wallet_closure.yaml}'
